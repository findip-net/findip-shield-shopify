# FindIP Shield Web Pixel

The Shopify Web Pixel extension for FindIP Shield. It subscribes to Shopify standard customer events in the strict sandbox and sends a privacy-minimized event envelope to FindIP Shield.

The only merchant setting is `siteKey`, a public FindIP Shield key beginning with `pub_`. The extension requests analytics processing only; marketing, preferences, and sale-of-data purposes are disabled.
